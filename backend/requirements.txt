# --- Core data / geo ---
pandas>=2.0
numpy>=1.24
geopandas>=0.14
shapely>=2.0
pyproj>=3.5
fiona>=1.9
rtree>=1.2    # 可选但强烈推荐：加速 sindex

# --- Viz ---
matplotlib>=3.7

# --- Media / imaging ---
opencv-python>=4.8
Pillow>=10.0

# --- Geospatial utils ---
geopy>=2.3

# --- Excel / Windows COM ---
openpyxl>=3.1
pywin32>=306   # 提供 pythoncom / win32com

# --- Networking ---
requests>=2.31

# --- Optional: UI / caching in gis_mapping ---
streamlit>=1.32

# --- Optional: CV auto-coding (prediction.py) ---
ultralytics>=8.0

# 如果 ultralytics 自动装 torch 不成功，手动解开下一行并选合适的版本/平台轮子：
# torch==2.2.*  # CPU: pip 官方轮子；GPU: 请用对应 CUDA/cuDNN 的 torch 轮子

Flask>=3.0
flask-cors>=4.0
exifread>=3.0    # 你后端 routes 里有 import exifread
